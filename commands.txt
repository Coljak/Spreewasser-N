

Usermanagement
https://www.youtube.com/watch?v=WuyKxdLcw3w

It is important to use python3! Otherwise crispy is not imported.!!!!

docker-compose run --rm app sh -c "python3 manage.py makemigrations"

requirements have changed:
docker-compose -f docker-compose.yml up --build -d

compose build --no-cache



BOOTSTRAP SASS:
cd into /app
// INSTALL
npm init -y
npm install bootstrap
npm install sass

// compile scss to css 
npx sass styles.scss bootstrap_scss.css

// https://blog.logrocket.com/how-switch-node-js-versions-nvm/
Into dockerfile: in app/
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

//source line added
export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm

#https://www.youtube.com/watch?v=nCX3QVl_PiI
npm install bootstrap@5.3


git checkout main
git pull origin main
git merge dev-3
git add <resolved files>
git commit 
git push
git checkout dev-3


docker logs -f swn_geo_django

docker-compose run --rm app sh -c "python3 manage.py runserver 0.0.0.0:8000"


This deletes build cache and unused images, but NOT your volumes. only affect images + cache:
docker builder prune -a
docker image prune -a


normal dev changes	up --build
dependency changes	build --no-cache && up
segfault / native crash	full rebuild + prune cache

docker compose stop app
docker compose rm -f   -----remove the whole thing
docker compose rm -f app   ------remove app

docker compose build --no-cache app


#### PRODUCTION !!!!
the command override the first docker-compose with the second
docker compose -f docker-compose.yml -f docker-compose.prod.yml up

Docker commands 
a) After changing docker-compose.(prod.)yml
docker compose up
b) After changing Dockerfile
docker compose up --build
c) Using production compose file

If file is docker-compose.prod.yml:

docker compose -f docker-compose.prod.yml up --build -d


DEBUG django while not running:
docker compose run --rm app sh

https://download.maxmind.com/geoip/databases/GeoLite2-ASN/download?suffix=tar.gz


https://download.maxmind.com/geoip/databases/GeoLite2-Country/download?suffix=tar.gz
https://download.maxmind.com/geoip/databases/GeoLite2-City/download?suffix=tar.gz